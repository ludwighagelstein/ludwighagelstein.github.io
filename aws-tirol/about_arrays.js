About Arrays:

    let farben = ["rot", "blau", "gruen"]
fabren.length; // Länge des Arrays = 3
farben[0]; //= "rot"
for (let i = 0; i < farben.length; i++) {
    console.log(farben[i]);
}



let farben = [
    [0, "blau"],
    [5, "rot"],
    [10, "gelb"]
]
fabren.length; // Länge des Arrays = 3
farben[2][1]; //= "rot"
for (let i = 0; i < farben.length; i++) {
    console.log(farben[i]);
}